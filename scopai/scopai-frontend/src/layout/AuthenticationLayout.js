// components/AuthenticationLayout.js
import React from 'react';

const AuthenticationLayout = ({ children }) => {
  return (
    <div>
      {/* You can customize the layout for authentication views here */}
      {children}
    </div>
  );
};

export default AuthenticationLayout;
